name: CI

on:
  workflow_dispatch:
  push:
    paths-ignore:
      - 'README.md'
      - 'CHANGES.md'
      - 'CLAUDE.md'
      - 'LICENSE'
      - 'THANKS'
      - '.github/workflows/claude.yml'
      - '.github/copilot-instructions.md'
      - 'sora-android-sdk/packages.md'
      - 'docs/**'
      - 'jitpack.yml'
      - 'canary.py'
      - '.gitignore'

permissions:
  actions: read

# concurrency:
#   group: ci-e2e-${{ github.ref }}
#   cancel-in-progress: true

jobs:
  e2e:

    runs-on: [self-hosted, macOS, ARM64, Apple-M1]
    timeout-minutes: 45
    env:
      SORA_SIGNALING_URL: ${{ secrets.TEST_SIGNALING_URL }}
      ANDROID_SDK_ROOT: /opt/android-sdk
      ADB_PATH: /opt/android-sdk/platform-tools/adb

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Set up JDK
        uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # v5.2.0
        with:
          # macOS arm64 の self-hosted runner でも利用可能な配布元を明示する。
          # runner ローカルの JDK 状態に依存せず、CI では常に JDK 17 を再現する。
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'
      - name: Validate Android SDK
        run: |
          test -n "${ANDROID_SDK_ROOT:-}" || { echo "ANDROID_SDK_ROOT is not set"; exit 1; }
          test -x "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" || {
            echo "sdkmanager not found at $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager"
            exit 1
          }
      - name: Copy gradle.properties
        run: cp gradle.properties.example gradle.properties
      - name: Validate E2E environment
        run: |
          test -n "${SORA_SIGNALING_URL:-}" || { echo "SORA_SIGNALING_URL is not set"; exit 1; }
      # - name: Accept SDK licenses
      #   run: yes | "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" --licenses
      # - name: Install Android SDK packages
      #   run: |
      #     "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" \
      #       "platform-tools" \
      #       "platforms;android-35" \
      #       "emulator" \
      #       "system-images;android-35;google_apis;arm64-v8a"
      - name: Start filtered logcat
        run: |
          nohup "$ADB_PATH" logcat -v time SoraE2ETest:D *:S \
            > sora-e2e-logcat.txt 2>&1 < /dev/null &
          echo $! > sora-e2e-logcat.pid
      - name: Run E2E tests
        run: ./gradlew :sora-android-sdk:pixelApi35AndroidE2ETest --stacktrace
      - name: Show SoraE2ETest logs
        if: always()
        run: |
          kill "$(cat sora-e2e-logcat.pid)" || true
          cat sora-e2e-logcat.txt || true
      - name: Dump adb diagnostics on failure
        if: failure()
        run: |
          "$ADB_PATH" devices || true
          "$ADB_PATH" logcat -d -b main -b system -b crash -v time || true

  # slack_notify:
  #   needs: [e2e]
  #   runs-on: ubuntu-slim
  #   if: always()
  #   steps:
  #     - name: Slack Notification
  #       uses: shiguredo/github-actions/.github/actions/slack-notify@main
  #       with:
  #         status: ${{ job.status }}
  ##         status: ${{ needs.e2e.result }}
  #         slack_webhook: ${{ secrets.SLACK_WEBHOOK }}
  #         slack_channel: sora-android-sdk
  #       env:
  #         GH_TOKEN: ${{ github.token }}
